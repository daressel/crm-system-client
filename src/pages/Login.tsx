import { FC } from "react";
import { Button } from "antd";

const LoginPage: FC = () => {
  return (
    <>
      <div>Login</div>
      <Button type="primary">Login</Button>
    </>
  );
};

export default LoginPage;
